package assignment188;

import java.util.Comparator;

public abstract class compareByName implements Comparator<Book> {
	public int compareByName(Book s1, Book s2)
	{
		return s1.getName().compareTo(s2.getName());
	}
}